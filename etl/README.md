# SSIS ETL

`Package.dtsx` demonstrates the original SSIS control flow and data-flow implementation. Machine-specific connection settings have been replaced with portable placeholders.

To run it locally, update the flat-file connection to `data/Orders.csv` and configure a SQL Server database named `SuperstoreDW`. Connection credentials are intentionally not included.
