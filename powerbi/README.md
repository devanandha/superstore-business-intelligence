# Power BI

`Superstore_Sales_Analysis.pbix` contains the dashboard and data model used to explore sales, profit, orders, monthly trends, category performance and returns.

If Power BI asks for a data source after download, point it to the local copy of `data/superstore.db` or the CSV files in `data/`, depending on the existing query configuration.
